#include <stdio.h>

int main() {
    int a;
    long b;
    long long c;
    float d;

    printf("size of a = %ld \n", sizeof(a));
    printf("size of b = %ld \n", sizeof(b));
    printf("size of c = %ld \n", sizeof(c));
    printf("size of d = %ld \n", sizeof(d));
}
